---
- name: Install ArgoCD
  hosts: k3s_cluster
  vars_files:
    - ./vars.yml
  run_once: true
  roles:
    - role: argocd
      vars:
        argocd__helm_values: "{{ argocd_helm_values | from_yaml }}"
        argocd__action: "deploy"
