name: lint

on: [push]

jobs:
  ansible-lint:
    uses: wittdennis/pipelines/.github/workflows/ansible-lint.yaml@1.11.29
    with:
      requirements-file: ansible/requirements.yml

  yaml-lint:
    uses: wittdennis/pipelines/.github/workflows/yaml-lint.yaml@1.11.29
    with:
      yamllint-path: .yamllint
