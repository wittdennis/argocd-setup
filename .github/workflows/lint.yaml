name: lint

on: [push]

jobs:
  ansible-lint:
    uses: wittdennis/pipelines/.github/workflows/ansible-lint.yaml@6b71882a90b71468a66605f934ef75a83bbe006b # 2.0.25
    with:
      requirements-file: ansible/requirements.yml

  yaml-lint:
    uses: wittdennis/pipelines/.github/workflows/yaml-lint.yaml@6b71882a90b71468a66605f934ef75a83bbe006b # 2.0.25
    with:
      yamllint-path: .yamllint
