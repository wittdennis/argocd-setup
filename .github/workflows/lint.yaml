name: lint

on: [push]

jobs:
  ansible-lint:
    uses: wittdennis/pipelines/.github/workflows/ansible-lint.yaml@ef870f2155acbf0282ee4b81501d3682ba2048d2 # 2.0.10
    with:
      requirements-file: ansible/requirements.yml

  yaml-lint:
    uses: wittdennis/pipelines/.github/workflows/yaml-lint.yaml@ef870f2155acbf0282ee4b81501d3682ba2048d2 # 2.0.10
    with:
      yamllint-path: .yamllint
