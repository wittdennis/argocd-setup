name: lint

on: [push]

jobs:
  ansible-lint:
    uses: wittdennis/pipelines/.github/workflows/ansible-lint.yaml@0851c942fac739c7f8bc82c5353bca1cc0aed00e # 2.0.12
    with:
      requirements-file: ansible/requirements.yml

  yaml-lint:
    uses: wittdennis/pipelines/.github/workflows/yaml-lint.yaml@0851c942fac739c7f8bc82c5353bca1cc0aed00e # 2.0.12
    with:
      yamllint-path: .yamllint
