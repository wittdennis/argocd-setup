name: lint

on: [push]

jobs:
  ansible-lint:
    uses: wittdennis/pipelines/.github/workflows/ansible-lint.yaml@0e01abfca7f024ab128b005993f093321808acb2 # 2.0.7
    with:
      requirements-file: ansible/requirements.yml

  yaml-lint:
    uses: wittdennis/pipelines/.github/workflows/yaml-lint.yaml@0e01abfca7f024ab128b005993f093321808acb2 # 2.0.7
    with:
      yamllint-path: .yamllint
